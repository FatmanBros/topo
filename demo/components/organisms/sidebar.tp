import { Logo } from "../atoms/logo.tp"
import { NavItem } from "../molecules/nav-item.tp"

Sidebar -> {
    style: "w-56 bg-white border-r border-gray-200 p-6 min-h-screen fixed left-0 top-0"
    align: vertical
    children: [
        Logo("topo", "text-3xl font-bold text-indigo-600 mb-8"),
        NavItem("Home"),
        NavItem("Getting Started"),
        NavItem("Syntax"),
        NavItem("Components"),
        NavItem("State"),
        NavItem("API Services")
    ]
}
