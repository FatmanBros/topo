import { SimpleCard } from "../molecules/card.tp"

Features -> {
    style: "grid grid-cols-3 gap-6 mb-16"
    children: [
        SimpleCard("Flat Definitions", "Define components at the top level. Compose by reference with children: [A, B, C]"),
        SimpleCard("NgRx-style State", "Built-in state with Actions, Reducers, Effects, and Selectors"),
        SimpleCard("Auto API Services", "Define REST endpoints, get CRUD methods auto-generated")
    ]
}
